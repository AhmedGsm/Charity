<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.charity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlacesDetailsActivity"
            android:label="@string/activity_details_name"/>
        <activity android:name=".SearchResultsActivity"
            android:label="@string/activity_search_name" />
        <activity android:name=".PlacesFindingActivity" />
        <activity android:name=".MainActivity">
            <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="AIzaSyCeOX-6wnF-i2hiWcwFRUWuqt4Cgbib7KA" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".database.PlaceContentProvider"
            android:authorities="com.example.charity"
            android:exported="false" />
    </application>

</manifest>